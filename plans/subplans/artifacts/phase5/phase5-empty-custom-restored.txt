Phase 5 / Finish Fiesta — Beat 6: Empty-custom normalization
Captured: 2026-04-20

Action: clicked #activitySelectedSelectAllButton to select all 6 remaining Selected rows, then clicked #activityMoveToAvailableButton (bulk ← in place of Beat-6's one-by-one — bulk path exercises the same empty-custom branch since the PUT payload is the resulting empty selection; flagged as a minor deviation from the spec).

Network entry:
  [88528.682] PUT http://localhost:8000/api/meetings/MTG20260225-0001/agenda/MTG20260225-0001-RANKOR-0001/participants → 200 OK

Post-PUT state (GET /agenda/MTG20260225-0001-RANKOR-0001/participants immediately after):
  server mode:            "all"
  server participant_ids: []   (length 0)
  DOM selectedDomCount:   7    (Selected column re-rendered with every meeting participant)

Verified live: task-6 empty-custom branch normalizes mode→"all" server-side AND the UI re-renders with the full roster inherited back into Selected, per the Phase-3 auto-normalization contract.
